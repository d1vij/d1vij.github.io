import boot from "@/assets/images/bsh-boot.png";
import demo from "@/assets/images/bsh-demo.png";

A TypeScript-based emulation of a UNIX-like shell featuring a custom command parsing and execution pipeline, an in-memory editable file system, and a modular object-oriented architecture designed for extensible command integration.

<Image src={boot} widthRem={80} />
<Image src={demo} widthRem={80} />
