import { generateBlogMetadata } from "@/lib";

export default generateBlogMetadata({
    title: "Rules and regulations (and features) while writing mdx",
    published: new Date(2026, 0, 12),
    tags: ["a", "foo", "barrr", "a", "barrr", "asdasjdasd"],
});
